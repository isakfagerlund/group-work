extends layout

block content
  .inner
    h2= title
    .schools
      each school in schools
        a(href=`/schools/${school._id}`)
          .document
            i.fas.fa-graduation-cap 
            span #{school.name}